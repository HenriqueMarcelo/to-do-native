import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
      backgroundColor: '#0D0D0D',
      alignItems: 'center',
      paddingTop: 70,
      paddingBottom: 70,
    },
  });