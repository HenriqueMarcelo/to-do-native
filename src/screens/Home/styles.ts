import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#1A1A1A',
  },
  subContainer: {
    paddingRight: 24,
    paddingLeft: 24,
  },
})
